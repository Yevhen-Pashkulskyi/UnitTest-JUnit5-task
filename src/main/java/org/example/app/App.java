package org.example.app;

import org.example.app.utils.AppStarter;

public class App {
    public static void main(String[] args) {
        AppStarter appStart= new AppStarter();
        appStart.getOutput(appStart.appStarter());
    }
}
